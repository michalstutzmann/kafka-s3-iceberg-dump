# Used for local `java -jar` / IDE runs. The Flink cluster image uses its own
# logging config under /opt/flink/conf and ignores this file.
rootLogger.level = INFO
rootLogger.appenderRef.console.ref = Console

appender.console.name = Console
appender.console.type = CONSOLE
appender.console.layout.type = PatternLayout
appender.console.layout.pattern = %d{HH:mm:ss.SSS} %-5p %c{1} - %m%n

logger.iceberg.name = org.apache.iceberg
logger.iceberg.level = INFO
logger.kafka.name = org.apache.kafka
logger.kafka.level = WARN
